[project]
name = "kozak-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "paramiko>=3.0.0",
    "python-dotenv>=1.0.0",
    "watchdog>=3.0.0",
    "websockets>=12.0",
]
